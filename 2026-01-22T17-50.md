#tochka #ssh #jump #psql

Сначала открываем туннель к прыжковому серверу
```bash
ssh -L 5433:db-host:5432 jump-user@jump-host -N
```
Далее подключаемся к тому порту локалхоста, к которому подсоединён туннель
```bash
psql -U db-user -h localhost -p 5433
```